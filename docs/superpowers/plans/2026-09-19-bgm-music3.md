# 音乐库（BGM · MiniMaxMusic3）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全局音乐库——Music3 生成（曲风/歌词三态）→ staging 试听 → 入库 → 项目引用 → 合成 amix 混入。

**Architecture:** 复刻音色库（voicelib/routes_voices）的 staging/confirm 心智：`engine/musiclib.py` 库 CRUD + LLM 双建议 + `gen_music` 队列任务；`music_library` 表 + projects 引用列；merge 末端 ffmpeg amix（视频流 copy 零重编码）。

**Tech Stack:** FastAPI + SQLite（迁移 43）+ ComfyClient + Vue3 设置页新 tab。

**Spec:** `docs/superpowers/specs/2026-09-18-bgm-music3-design.md`

## Global Constraints

- engine 禁 import fastapi/starlette/uvicorn
- 测试反模式：LLM 用 FakeClient、ComfyUI 用 tests/comfy_mock.py 的 comfy_server、API 用 create_app(start_workers=False)
- UPDATE/DELETE 带 LIMIT 只能放子查询（Windows sqlite）
- DB 路径存相对 data 的 POSIX（engine/paths.py rel_to_data）
- 每任务 pytest 全绿后 commit（小步提交）

---

### Task 1: music3 模板 + 映射键

**Files:**
- Create: `templates/workflows/music3.api.json`、`templates/workflows/music3.yaml`
- Modify: `comic_studio/engine/settings.py`（DEFAULT_SETTINGS template_map 加 `"music": "music3"`）
- Modify: `comic_studio/web/routes_settings.py`（TEMPLATE_MAP_KEYS 加 `"music"`）
- Test: `tests/test_musiclib.py`（新建，本任务只加模板测试）

**Interfaces:**
- Produces: 模板 id `music3`、type `music`；inject：prompt→`37:13.caption`、
  seed→`37:38.seed`、`max_duration`→`37:13.max_duration`（FLOAT）、
  `lyrics`→`37:13.lyrics`（STRING，空=纯音乐）；outputs→`35`（SaveAudioAdvanced）；
  models：unet `37:6`/clip `37:3`/vae `37:7`

- [ ] **Step 1: 生成模板文件**（转换脚本一次跑）

```python
# 一次性脚本（bash 里跑，不入库）
import json
raw = json.load(open('templates/workflows/_raw/audio_minimax_music_3.json'))
raw['35']['inputs']['filename_prefix'] = 'cs/{project}/{asset}'
json.dump(raw, open('templates/workflows/music3.api.json', 'w', encoding='utf-8'),
          ensure_ascii=False, indent=1)
```

`music3.yaml`：

```yaml
id: music3
type: music
name: MiniMax Music3 音乐生成
file: music3.api.json
prompt_format: '{prompt}'
inject:
  prompt:
    node: '37:13'
    field: caption
  params:
    seed:
      node: '37:38'
      field: seed
    max_duration:
      node: '37:13'
      field: max_duration
    lyrics:
      node: '37:13'
      field: lyrics
outputs:
- node: '35'
  filename_prefix: cs/{project}/{asset}
requires: []
models:
- {label: unet, node: '37:6', field: unet_name, cls: UNETLoader, label_cn: 音乐底模}
- {label: clip, node: '37:3', field: clip_name, cls: CLIPLoader, label_cn: 音乐文本编码器}
- {label: vae, node: '37:7', field: vae_name, cls: VAELoader, label_cn: 音频 VAE}
```

- [ ] **Step 2: 写失败测试** `tests/test_musiclib.py`

```python
# tests/test_musiclib.py
"""音乐库（2026-09-19 spec）：music3 模板 / 库 CRUD / LLM 建议 / gen_music /
API / 合成混入。本文件随任务逐段追加。"""
import json
from pathlib import Path

from comic_studio.engine.workflows.registry import scan_templates


def test_music3_template():
    reg = scan_templates(Path("templates/workflows"))
    t = reg["music3"]
    assert t.type == "music"
    wf = t.api_json()
    assert wf["37:13"]["class_type"] == "MiniMaxMusic3TextEncode"
    assert wf["35"]["class_type"] == "SaveAudioAdvanced"
    assert t.inject_prompt.node == "37:13" and t.inject_prompt.field == "caption"
    for key, node, field in (("seed", "37:38", "seed"),
                             ("max_duration", "37:13", "max_duration"),
                             ("lyrics", "37:13", "lyrics")):
        ip = t.inject_params[key]
        assert (ip.node, ip.field) == (node, field), key
    assert {s.label for s in t.models} == {"unet", "clip", "vae"}
```

- [ ] **Step 3: 跑测试确认失败→改 settings/routes→通过**

Run: `.venv/bin/python -m pytest tests/test_musiclib.py -q`（先 FAIL: music3 未注册；
加 settings.py `"music": "music3"` 与 TEMPLATE_MAP_KEYS `"music"` 后 PASS）

- [ ] **Step 4: Commit**

```bash
git add templates/workflows/music3.* comic_studio/engine/settings.py \
  comic_studio/web/routes_settings.py tests/test_musiclib.py
git commit -m "feat: music3 模板入库（caption/lyrics/max_duration 三注入）+ music 映射键"
```

---

### Task 2: 迁移 43 + musiclib 库 CRUD

**Files:**
- Modify: `comic_studio/engine/db.py`（MIGRATIONS 追加第 43 条）
- Create: `comic_studio/engine/musiclib.py`
- Test: `tests/test_musiclib.py` 追加

**Interfaces:**
- Produces（后续任务依赖的签名）:
  - `save_to_library(db, data_dir, src: Path, name: str, caption: str, lyrics: str, seed: int, duration: float, origin: str = "user") -> dict`（重名 `ValueError("音乐名已存在: …")`）
  - `list_music(db) -> list[dict]`（含 `id/name/caption/lyrics/seed/duration/origin/path/created_at`，path=相对 data POSIX）
  - `delete_music(db, data_dir, music_id: int) -> None`（不存在 `ValueError("音乐不存在: …")`）
  - `STAGING_REL = "music/_staging"`、`staging_dir(data_dir) -> Path`

- [ ] **Step 1: 迁移 43（失败测试先行）**——db.py MIGRATIONS 列表末尾追加：

```python
    """CREATE TABLE IF NOT EXISTS music_library (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL UNIQUE,
        caption TEXT NOT NULL DEFAULT '',
        lyrics TEXT NOT NULL DEFAULT '',
        seed INTEGER NOT NULL DEFAULT 0,
        duration REAL NOT NULL DEFAULT 120,
        origin TEXT NOT NULL DEFAULT 'user',
        path TEXT NOT NULL,
        created_at TEXT NOT NULL DEFAULT (datetime('now','localtime'))
    );
    ALTER TABLE projects ADD COLUMN bgm_music_id INTEGER;
    ALTER TABLE projects ADD COLUMN bgm_volume REAL NOT NULL DEFAULT 0.2;""",
```

测试（追加到 test_musiclib.py）：

```python
def test_migrate_43_music_table(tmp_path):
    from comic_studio.engine.db import Database
    db = Database(tmp_path / "s.db"); db.migrate()
    conn = db.connect()
    cols = [r["name"] for r in conn.execute("PRAGMA table_info(music_library)")]
    assert {"id", "name", "caption", "lyrics", "seed", "duration",
            "origin", "path", "created_at"} <= set(cols)
    pcols = [r["name"] for r in conn.execute("PRAGMA table_info(projects)")]
    assert "bgm_music_id" in pcols and "bgm_volume" in pcols
```

- [ ] **Step 2: musiclib.py CRUD 实现**（骨架 + 三个函数；`rel_to_data` 从 `..paths` 导入；
文件落 `data/music/custom/<name>.mp3`，`data_dir/music/custom` mkdir parents）

```python
# comic_studio/engine/musiclib.py
"""音乐库（2026-09-19 spec）：Music3 生成 → staging 试听 → 入库 → 项目引用。
同 voicelib 心智：文件在 data/music/custom，元数据在 music_library 表。"""
import shutil
from pathlib import Path

STAGING_REL = "music/_staging"
LIBRARY_REL = "music/custom"


def staging_dir(data_dir) -> Path:
    d = Path(data_dir) / STAGING_REL
    d.mkdir(parents=True, exist_ok=True)
    return d


def save_to_library(db, data_dir, src: Path, name: str, caption: str, lyrics: str,
                    seed: int, duration: float, origin: str = "user") -> dict:
    name = (name or "").strip()
    if not name:
        raise ValueError("音乐名不能为空")
    conn = db.connect()
    if conn.execute("SELECT 1 FROM music_library WHERE name=?", (name,)).fetchone():
        raise ValueError(f"音乐名已存在: {name}")
    lib = Path(data_dir) / LIBRARY_REL
    lib.mkdir(parents=True, exist_ok=True)
    dest = lib / f"{name}{src.suffix or '.mp3'}"
    shutil.copy2(src, dest)
    from .paths import rel_to_data
    rel = rel_to_data(data_dir, dest)
    cur = conn.execute(
        "INSERT INTO music_library (name, caption, lyrics, seed, duration, origin, path) "
        "VALUES (?,?,?,?,?,?,?)",
        (name, caption, lyrics, int(seed), float(duration), origin, rel))
    conn.commit()
    return {"id": cur.lastrowid, "name": name, "path": rel}


def list_music(db) -> list[dict]:
    rows = db.connect().execute(
        "SELECT * FROM music_library ORDER BY id DESC").fetchall()
    return [dict(r) for r in rows]


def delete_music(db, data_dir, music_id: int) -> None:
    conn = db.connect()
    row = conn.execute("SELECT * FROM music_library WHERE id=?",
                       (music_id,)).fetchone()
    if row is None:
        raise ValueError(f"音乐不存在: {music_id}")
    from .paths import data_to_abs
    p = data_to_abs(data_dir, row["path"])
    p.unlink(missing_ok=True)
    conn.execute("DELETE FROM music_library WHERE id=?", (music_id,))
    conn.commit()
```

- [ ] **Step 3: CRUD 测试**（追加；注意 `db.connect().row_factory` 已是 sqlite3.Row）

```python
def test_library_crud_roundtrip(tmp_path):
    from comic_studio.engine.db import Database
    from comic_studio.engine.musiclib import (delete_music, list_music,
                                              save_to_library)
    db = Database(tmp_path / "s.db"); db.migrate()
    src = tmp_path / "draft.mp3"; src.write_bytes(b"mp3data")
    entry = save_to_library(db, tmp_path, src, "夜晚钢琴", "Lo-fi 钢琴 70BPM",
                            "", 42, 120)
    assert entry["id"] and (tmp_path / "music/custom/夜晚钢琴.mp3").exists()
    assert list_music(db)[0]["name"] == "夜晚钢琴"
    with pytest.raises(ValueError, match="已存在"):
        save_to_library(db, tmp_path, src, "夜晚钢琴", "", "", 1, 60)
    delete_music(db, tmp_path, entry["id"])
    assert list_music(db) == [] and not (tmp_path / "music/custom/夜晚钢琴.mp3").exists()
    with pytest.raises(ValueError, match="音乐不存在"):
        delete_music(db, tmp_path, entry["id"])
```

- [ ] **Step 4: 跑测试 + Commit**

Run: `.venv/bin/python -m pytest tests/test_musiclib.py -q` → PASS

```bash
git add comic_studio/engine/db.py comic_studio/engine/musiclib.py tests/test_musiclib.py
git commit -m "feat: 迁移 43 music_library 表 + projects 配乐两列；musiclib 库 CRUD"
```

---

### Task 3: LLM 双建议（曲风/歌词）

**Files:**
- Modify: `comic_studio/engine/musiclib.py`
- Test: `tests/test_musiclib.py` 追加

**Interfaces:**
- Consumes: `comic_studio.engine.llm.provider.client_for_task(db, "gen_story")`（FakeClient 替换 raw_chat）
- Produces: `suggest_caption(db, hint: str = "") -> str`、`suggest_lyrics(db, hint: str = "") -> str`（空输出 `ValueError`）

- [ ] **Step 1: 失败测试**

```python
class FakeLLM:
    def __init__(self, reply): self.reply, self.calls = reply, []
    def raw_chat(self, messages, temperature=0.5):
        self.calls.append(messages); return self.reply, None


def test_suggest_caption_and_lyrics(tmp_path, monkeypatch):
    from comic_studio.engine.db import Database
    from comic_studio.engine import musiclib
    db = Database(tmp_path / "s.db"); db.migrate()
    fake = FakeLLM("Global Metadata: Cinematic lo-fi, 72 BPM, A minor.\n情绪走向：由静谧渐至温暖。")
    monkeypatch.setattr(musiclib, "client_for_task", lambda db, task: fake)
    cap = musiclib.suggest_caption(db, "雨夜重逢的校园恋爱短剧")
    assert cap.startswith("Global Metadata") and "雨夜" in fake.calls[0][-1]["content"]
    fake2 = FakeLLM("[主歌]\n风穿过走廊\n[副歌]\n我想再见你一面")
    monkeypatch.setattr(musiclib, "client_for_task", lambda db, task: fake2)
    assert "[副歌]" in musiclib.suggest_lyrics(db, "毕业季告别")
    with pytest.raises(ValueError, match="曲风"):
        monkeypatch.setattr(musiclib, "client_for_task",
                            lambda db, task: FakeLLM("  "))
        musiclib.suggest_caption(db)
```

- [ ] **Step 2: 实现**（musiclib.py 追加；`from .llm.provider import client_for_task`
放模块顶层；系统词中文、要求直出正文无解释；lyrics 系统词给主歌/副歌结构与押韵要求；
user 拼 hint）

```python
def suggest_caption(db, hint: str = "") -> str:
    system = ("你是音乐曲风描述写手，为 MiniMax Music3 生成 caption。输出格式："
              "第一行 'Global Metadata: <风格>, <BPM>, <调性>.'，第二行起中文描述"
              "情绪走向与配器（2~3 句）。只输出正文，不要解释。")
    user = f"作品语境：{hint or '通用背景音乐'}"
    from .llm.provider import client_for_task
    text, _ = client_for_task(db, "gen_story").raw_chat(
        [{"role": "system", "content": system},
         {"role": "user", "content": user}], temperature=0.5)
    text = (text or "").strip()
    if not text:
        raise ValueError("曲风建议为空，请重试")
    return text


def suggest_lyrics(db, hint: str = "") -> str:
    system = ("你是歌词作者。输出中文歌词，结构为 [主歌] / [副歌]（可含 [桥段]），"
              "每段 4 行内、口语可唱、末字尽量押韵。只输出歌词正文。")
    user = f"主题：{hint or '青春、遗憾与重逢'}"
    from .llm.provider import client_for_task
    text, _ = client_for_task(db, "gen_story").raw_chat(
        [{"role": "system", "content": system},
         {"role": "user", "content": user}], temperature=0.7)
    text = (text or "").strip()
    if not text:
        raise ValueError("歌词建议为空，请重试")
    return text
```

（注：`client_for_task` 顶部导入供 monkeypatch 命中——模块内引用统一走
`musiclib.client_for_task`，两函数内的 `from .llm.provider import` 删掉，
只在模块顶部 import 一次。）

- [ ] **Step 3: 跑测试 + Commit**

```bash
.venv/bin/python -m pytest tests/test_musiclib.py -q
git add comic_studio/engine/musiclib.py tests/test_musiclib.py
git commit -m "feat: musiclib LLM 双建议——曲风 caption / 中文歌词"
```

---

### Task 4: gen_music 处理器 + staging

**Files:**
- Modify: `comic_studio/engine/voicelib.py`（`_run_template` 加 `prompt: str | None = None` 形参并透传 fill_workflow——默认 None 零行为变化）
- Modify: `comic_studio/engine/musiclib.py`（`handle_gen_music`）
- Test: `tests/test_musiclib.py` 追加

**Interfaces:**
- Consumes: voicelib `_run_template(comfy, template_id, params, images, dest_dir, name, db=None, prompt=None)`；queue `enqueue_job(db, "gen_music", project_id=?, resource="gpu_comfy", payload={...})`；jobs `attach_snapshot`
- Produces: `@register("gen_music") handle_gen_music(db, data_dir, job, comfy)`；payload
  `{caption: str, lyrics: str, seed: int, duration: float}`；staging 文件
  `music/_staging/<job_id>.mp3`；job `snapshot_json` 记录 staging 相对路径（save 端点回读）

- [ ] **Step 1: 失败测试**（comfy_mock；镜像 tests 里 voicelib 生成用例的构造方式——
`comfy_server("ok", audio=True)`，看 tests/test_api_voices.py 现有用法照抄参数）

```python
def test_gen_music_handler(tmp_path, monkeypatch):
    from comic_studio.engine.comfy.client import ComfyClient
    from comic_studio.engine.db import Database
    from comic_studio.engine import musiclib
    from comic_studio.engine.jobs import enqueue_job, get_job
    from comic_studio.engine.workflows import registry
    from comfy_mock import comfy_server
    db = Database(tmp_path / "s.db"); db.migrate()
    monkeypatch.setattr(registry, "TEMPLATE_ROOT", Path("templates/workflows"))
    jid = enqueue_job(db, "gen_music", project_id=None, resource="gpu_comfy",
                      payload={"caption": "Global Metadata: lo-fi, 70 BPM.",
                               "lyrics": "[主歌]\n夜色温柔", "seed": 7,
                               "duration": 60})
    with comfy_server("ok", audio=True) as m:   # 参数以 test_api_voices.py 现役写法为准
        musiclib.handle_gen_music(db, tmp_path, get_job(db, jid),
                                  ComfyClient(m.base_url))
        wf = m.prompts[0]["prompt"]["37:13"]["inputs"]
        assert wf["caption"].startswith("Global Metadata")
        assert wf["lyrics"].startswith("[主歌]") and wf["max_duration"] == 60
        assert wf["seed"] == 7
    st = musiclib.staging_dir(tmp_path) / f"{jid}.mp3"
    assert st.exists()
    snap = json.loads(get_job(db, jid)["snapshot_json"])
    assert snap.get("staging") == str(st.relative_to(tmp_path))
```

- [ ] **Step 2: voicelib._run_template 加 prompt 形参**（一处签名 + fill_workflow
调用传 `prompt=prompt`；既有调用不传=行为不变；跑 `pytest tests/test_voices*.py
tests/test_api_voices.py -q` 全绿确认）

- [ ] **Step 3: handle_gen_music 实现**（musiclib.py 追加）

```python
def handle_gen_music(db, data_dir, job, comfy) -> Path:
    from .queue.worker import register
    from .jobs import attach_snapshot
    payload = json.loads(job["payload_json"] or "{}")
    caption = (payload.get("caption") or "").strip()
    if not caption:
        raise ValueError("gen_music 缺 caption")
    lyrics = (payload.get("lyrics") or "").strip()
    duration = min(360.0, max(15.0, float(payload.get("duration") or 120)))
    seed = int(payload.get("seed") or 0)
    from .voicelib import _run_template
    dest = _run_template(
        comfy, "music3",
        params={"seed": seed, "max_duration": duration, "lyrics": lyrics},
        images=None, dest_dir=staging_dir(data_dir), name=str(job["id"]),
        db=db, prompt=caption)
    from .paths import rel_to_data
    rel = rel_to_data(data_dir, dest)
    attach_snapshot(db, job["id"], prompt=caption, workflow={"staging": rel})
    return dest
```

并在模块底部 `from .queue.worker import register` 装饰：
`register("gen_music")(handle_gen_music)`（参照 genref 的 `@register` 挂法——
queue.worker 在 handler 定义处 import，注意循环导入：放函数外模块级、文件底部）。

- [ ] **Step 4: 跑测试 + Commit**

```bash
.venv/bin/python -m pytest tests/test_musiclib.py tests/test_voices.py tests/test_api_voices.py -q
git add comic_studio/engine/musiclib.py comic_studio/engine/voicelib.py tests/test_musiclib.py
git commit -m "feat: gen_music 处理器——Music3 staging 生成（voicelib._run_template 加 prompt 透传）"
```

---

### Task 5: routes_music API

**Files:**
- Create: `comic_studio/web/routes_music.py`
- Modify: `comic_studio/web/app.py`（include router——找 routes_voices 的 include 处照抄）
- Test: `tests/test_musiclib.py` 追加（API 段）

**Interfaces:**
- Produces:
  - `POST /api/music/generate` body `{caption, lyrics?, seed?, duration?}` → 202 `{job_id}`；已有 pending/running gen_music → 409；caption 空 → 422
  - `POST /api/music/suggest-caption` / `suggest-lyrics` body `{hint?}` → `{text}`（LLM 异常 502 包 detail）
  - `GET /api/music` → `{music: [...], staging: {job_id, status, path?}[]}`（staging=最近 gen_music job 快照里的 staging 文件且磁盘存在）
  - `POST /api/music/save` body `{job_id, name, caption, lyrics, seed, duration}` → 201 `{id,...}`；staging 文件不在 409；重名 409（ValueError 转）
  - `POST /api/music/discard` body `{job_id}` → 204（删 staging 文件）
  - `DELETE /api/music/{music_id}` → 204；不存在 404

- [ ] **Step 1: 失败测试**（create_app(start_workers=False)；generate 只验入队
202/409/422——用 jobs 表断言 type=gen_music 已入队；save/discard 用假 staging 文件；
suggest 用 monkeypatch 打 musiclib.suggest_*）

```python
def test_music_api_matrix(tmp_path, monkeypatch):
    from fastapi.testclient import TestClient
    from comic_studio.engine.db import Database
    from comic_studio.engine import musiclib
    from comic_studio.web.app import create_app
    db = Database(tmp_path / "s.db"); db.migrate()
    with TestClient(create_app(tmp_path / "s.db", tmp_path / "data",
                               start_workers=False)) as c:
        r = c.post("/api/music/generate", json={"caption": ""})
        assert r.status_code == 422
        r = c.post("/api/music/generate",
                   json={"caption": "Global Metadata: lo-fi.", "seed": 3,
                         "duration": 60})
        assert r.status_code == 201 and r.json()["job_id"]
        r2 = c.post("/api/music/generate",
                    json={"caption": "Global Metadata: lo-fi."})
        assert r2.status_code == 409
        # save：造 staging
        jid = r.json()["job_id"]
        st = musiclib.staging_dir(tmp_path / "data") / f"{jid}.mp3"
        st.write_bytes(b"mp3")
        from comic_studio.engine.jobs import attach_snapshot
        attach_snapshot(db, jid, staging=str(st.relative_to(tmp_path / "data")))
        monkeypatch.setattr(musiclib, "save_to_library",
                            lambda *a, **k: {"id": 1, "name": "n", "path": "p"})
        r3 = c.post("/api/music/save", json={"job_id": jid, "name": "夜曲",
                                             "caption": "c", "lyrics": "",
                                             "seed": 1, "duration": 60})
        assert r3.status_code == 201
        r4 = c.post("/api/music/save", json={"job_id": 999, "name": "x",
                                             "caption": "", "lyrics": "",
                                             "seed": 1, "duration": 60})
        assert r4.status_code == 409
```

（generate 返回码若项目现役惯例是 202，以 routes_voices 既有入队端点的返回码为准对齐。）

- [ ] **Step 2: 路由实现**（routes_music.py；在飞互斥查 jobs：`type='gen_music' AND
status IN ('pending','running')`；save 前 staging 路径必须来自**该 job 的 snapshot**
并做 `_safe` 相对路径校验（参照 use-kf 的路径穿越防线——`Path(rel)` 解析后必须落在
data_dir 下）；HTTPException 从 fastapi 导入——web 层允许）

- [ ] **Step 3: 跑测试（含全套 API 套件）+ Commit**

```bash
.venv/bin/python -m pytest tests/test_musiclib.py tests/test_api_settings.py -q
git add comic_studio/web/routes_music.py comic_studio/web/app.py tests/test_musiclib.py
git commit -m "feat: /api/music 全套——generate/suggest/list/save/discard/delete"
```

---

### Task 6: 项目引用 + 合成混入

**Files:**
- Modify: `comic_studio/engine/merge.py`（`_mix_bgm` + merge_project 末段调用）
- Modify: `comic_studio/engine/director_mix.py`（mix_director_audio 之后同函数）
- Modify: `comic_studio/web/routes_projects.py`（`PATCH /{id}/bgm` 小端点）
- Test: `tests/test_musiclib.py` 追加（混音三态）；`tests/test_api_projects.py` 追加（PATCH）

**Interfaces:**
- Produces:
  - `merge._mix_bgm(db, data_dir, proj, final: Path) -> Path`：proj.bgm_music_id 空/库文件缺 → 原样返回 final；有 → amix 出 `final.with_name(final.stem + '_bgm.mp4')` 并覆盖引用
  - `PATCH /api/projects/{id}/bgm` body `{music_id: int|None, volume: float}`：music_id 非空须存在于 music_library 否则 422；volume 钳 0~0.5

- [ ] **Step 1: _mix_bgm 失败测试**（mock ffmpeg——看 tests/test_merge.py 现役的
ffmpeg 替身手法照抄；三态：无引用原样返回 / 有引用产出 amix 命令含
`-stream_loop -1` + `volume=0.2` + `amix=inputs=2:duration=first` + `-c:v copy` /
文件缺失 warn 降级原样返回）

```python
def test_mix_bgm_three_states(tmp_path):
    # 具体断言以 tests/test_merge.py 的 ffmpeg mock 断言风格为准
    ...
```

- [ ] **Step 2: 实现**（ffmpeg 模板）：

```python
def _mix_bgm(db, data_dir, proj, final: Path) -> Path:
    mid = proj["bgm_music_id"] if "bgm_music_id" in proj.keys() else None
    if not mid:
        return final
    row = db.connect().execute("SELECT * FROM music_library WHERE id=?",
                               (mid,)).fetchone()
    if row is None:
        return final
    from .paths import data_to_abs
    music = data_to_abs(data_dir, row["path"])
    if not music.exists():
        emit_log(db, "comfy", "warn", f"配乐文件缺失，跳过混音: {music}")
        return final
    vol = min(0.5, max(0.0, float(proj["bgm_volume"] if "bgm_volume" in proj.keys() else 0.2)))
    out = final.with_name(final.stem + "_bgm.mp4")
    cmd = [ffmpeg_bin(), "-y", "-i", str(final), "-stream_loop", "-1",
           "-i", str(music),
           "-filter_complex",
           f"[1:a]volume={vol}[bg];[0:a][bg]amix=inputs=2:duration=first[a]",
           "-map", "0:v", "-map", "[a]", "-c:v", "copy", "-c:a", "aac",
           str(out)]
    subprocess.run(cmd, check=True, capture_output=True)
    shutil.move(out, final)   # 覆盖引用——epNNN.mp4 即成片
    return final
```

调用点：merge_project 在产出最终 mp4、set_stage("merged") **之前**插
`final = _mix_bgm(db, data_dir, proj, final)`；director_mix 同构（函数在
mix_director_audio 返回后调用；proj 行经 db 传入）。

- [ ] **Step 3: PATCH /bgm 端点 + 测试**（routes_projects 照 patchComicParam 风格小端点；
music_id=None 清引用）

- [ ] **Step 4: 跑测试（含 tests/test_merge.py 全量回归）+ Commit**

```bash
.venv/bin/python -m pytest tests/test_musiclib.py tests/test_merge.py tests/test_api_projects.py -q
git add comic_studio/engine/merge.py comic_studio/engine/director_mix.py \
  comic_studio/web/routes_projects.py tests/
git commit -m "feat: 项目配乐引用 + 成片 amix 混入（视频流 copy；快车道同构）"
```

---

### Task 7: 前端

**Files:**
- Modify: `frontend/index.html`（设置页 tabs 数组加 `{id:'music',name:'🎵 音乐库'}`；
对应 panel：生成表单+staging 试听+库列表；项目参数面板加配乐下拉+音量）
- Modify: `frontend/app.js`（data: `musicLib: []`、`musicForm`、`musicStaging`、
`musicBusy`；methods：`musicSuggestCaption/musicSuggestLyrics/musicGenerate/
musicSave/musicDiscard/musicDelete/loadMusic`；项目面板 `patchBgm`）
- Test: 手动 Playwright 验证（无前端单测基建）——375px 无横向溢出扫一眼

要点（实现时照「🎵 音色库」tab 的 markup/状态机照抄改字段）：
- 生成表单：caption textarea + ✨按钮、lyrics textarea + ✨按钮 + 「留空=纯音乐」
  占位、时长 number（15~360）、🎲 按钮（随机 seed）
- staging 区：`<audio controls :src="...">`（/media/music/_staging/…）+ 名称输入 +
  「✓ 保存入库」「✕ 放弃」
- 库列表：每行试听 `<audio>` + 名称 + 🗑
- 项目参数面板：`<select v-model="proj.bgm_music_id">`（选项= musicLib）+ 音量
  `<input type=range min=0 max=0.5 step=0.05>`，change 调 `PATCH /api/projects/{id}/bgm`
- merged 后改选提示「重新合成生效」（复用既有 toast/文案模式）
- 判例遵守：模板里调用的助手必须进 methods（不是 computed）

- [ ] **Step 1: 实现**  Step 2: 手动验证（服务重启后设置页出 tab、生成→试听→入库→项目选曲全流程）
Step 3: Commit `git commit -m "feat: 设置页音乐库 tab + 项目配乐选择"`

---

### Task 8: 文档

- CLAUDE.md 模块地图新条目（音乐库全链 + 判例：staging 路径校验/merge 末端混音零重编码）
- templates/workflows/README.md 模板表加 music3 行
- .claude/skills/comic-studio/REFERENCE.md 音频面补一句音乐库
- Commit `docs: 音乐库落地文档同步`

---

## Self-Review 结论

- spec 覆盖：模板✓ 库CRUD✓ LLM 双建议✓ gen_music✓ API✓ 项目引用✓ 合成混入（含快车道）✓ 前端✓ 文档✓；TRT 判例不属本计划
- spec 中「merge_cache 键纳入 music」已证不必要：merge_cache 是**段级**缓存，BGM 混音发生在 concat 之后的最终产物上，天然不受影响（计划不再动缓存键，文档注明）
- 迁移号核对：MIGRATIONS 当前 42 条 → 本计划 43（表+列合一）
